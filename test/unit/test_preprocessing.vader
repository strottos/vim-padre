" vim: et ts=2 sts=2 sw=2

Before:
  call padre#Enable()

After:
  Restore

Execute (We can create a preprocessing window):
  call padre#preprocessing#CreatePreprocessingWindow()

  AssertEqual 2, winnr('$')

  wincmd b

Then:
  AssertEqual bufnr('%'), padre#buffer#GetBufNumForBufName('PADRE_Preprocessing')

  call padre#layout#CloseTabsWithBuffer('PADRE_Main')
